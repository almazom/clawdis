# Tests

- Unit tests are colocated with sources as `src/**/*.test.ts`.
- E2E tests use `*.e2e.test.ts` (rare, focused on integration flows).
- Shared fixtures live under `tests/fixtures/`.
- Run `pnpm test` or `pnpm test:coverage` (see `docs/test.md`).
